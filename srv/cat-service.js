const cds= require('@sap/cds');
const {SELECT,INSERT} = cds.ql;
const {Books} = cds.entities("com.temp");

// discount on the book if the amount of the 
module.exports = function(){                    // exporting the module
    
}